# Nginx — SPA (React Router) fallback so refresh on /admin, /login, etc. works.
# Use inside server { } for the site that serves the Vite build (dist/), not the API upstream.
#
#   root /var/www/raxwo-client/dist;
#   index index.html;
#
#   location / {
#     try_files $uri $uri/ /index.html;
#   }
#
# Keep API on another server_name or path, e.g. location /api { proxy_pass http://127.0.0.1:5000; }
